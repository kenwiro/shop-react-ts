import React from 'react';

const CartSectionTitle = (): JSX.Element => {
  return (
    <p className={'cart__body__content__section_title'}>Оформить заказ</p>
  );
};

export default CartSectionTitle;
